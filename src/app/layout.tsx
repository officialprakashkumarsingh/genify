import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "Genify - AI Web App Generator",
  description: "Generate complete web applications with AI. Create, customize, and deploy instantly.",
  keywords: ["AI", "web development", "code generation", "web app generator"],
  authors: [{ name: "Genify" }],
  viewport: "width=device-width, initial-scale=1",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
      </body>
    </html>
  );
}